name: CI

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  ci:
    uses: achimdehnert/platform/.github/workflows/_ci-python.yml@session/2026-06-12/achim-dehnert/sharedci-ephemeral-pg-port
    with:
      skip_tests: false
      django_settings_module: "config.settings.test"
    secrets: inherit